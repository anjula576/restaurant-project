spring.application.name=restaurant
spring.datasource.url=jdbc:mysql://localhost:3306/restaurant_db
spring.datasource.username=root
spring.datasource.password=Anjula12345

# MySQL local database

# to configure mail server
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# add email that use to send email
spring.mail.username=anjula.183.gcc@gmail.com
# app password (this password automatically generated when created a app in gmail)
spring.mail.password=wtfe cvcr rtat kycw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# image upload path

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
# add the path where images are stored
# relative to the project root directory (don't use static path here)
#  deployment environment use different paths so use relative path (this path is relative to the project root directory)
image.upload.path=uploads/images/
